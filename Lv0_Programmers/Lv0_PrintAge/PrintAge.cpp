#include <iostream>

using namespace std;

// ???? solution ??
int solution(int age) {
    int answer = 2022 - age + 1;
    return answer;
}

// IDE?? ??? ?? main ??
int main() {
    int age;
    
    // 1. ?? ??
    cout << "2022? ?? ??? ????? (?: 40 ?? 23): ";
    cin >> age;
    
    // ???? ?? (0 < age <= 120)
    if (age <= 0 || age > 120) {
        cout << "??? 1? ?? 120? ??? ??????." << endl;
        return 0; // ???? ??
    }
    
    // 2. solution ?? ??
    int birth_year = solution(age);
    
    // 3. ?? ????
    cout << "2022? ?? " << age << "???? " << birth_year << "?????." << endl;

    return 0;
}