#include <iostream>
#include <vector>
#include <cstdlib>
#include <ctime>

using namespace std;

int main_random()
{
    
    /*숫자 야구게임 방법
     * 1. 처음에 숫자 9개를 두고 3개를 정한다.
     * 2. 나와 상대편 서로 3개씩 숫자를 뽑는다. 
     * 3. 내가 상대편의 숫자를 3개를 다 불러서 위치와 숫자가 맞으면 스트라이크, 숫자만 맞으면 볼, 아무것도아니면 아웃을 출력한다
     * 4. 그래서 1스트라이크 1볼 1아웃이면 1S 1B 1O 이런식으로 출력한다.
     * 5. 전부 스트라이크가 되면 승리한다.
     */
    
    // 1. 1 ~ 9 사이의 랜덤한 숫자 3개를 얻어온다. (단, 숫자는 중복되어서는 안된다.)
 
    // 2. 게임 시작을 알리며 맞춰야 할 3개의 숫자를 "* * *" 형태로 출력한다.
    
    // 3. 사용자가 3개의 숫자를 모두 맞출 때까지 계속해서 입력을 받는 반복문을 작성한다.

        // 4. 사용자로부터 3개의 숫자를 입력받는다.
        
        // 5. 만약 입력받은 숫자 3개 중 한 개라도 0이 있을 경우 게임을 종료(break)한다.
        
        // 6. 스트라이크와 볼을 계산하기 전 카운트를 초기화한다.
        
        // 7. 입력받은 숫자와 정답을 비교하여 스트라이크와 볼을 계산한다.
        //    - 숫자와 위치가 모두 같으면 strike
        //    - 숫자는 맞지만 위치가 다르면 ball
        
        // 8. 계산된 결과를 조건에 맞게 출력한다.
        //    - 아무것도 맞추지 못했을 경우(0strike 0ball): "Out" 출력
        //    - 그 외의 경우: "Xstrike Yball" 출력 (예: "1strike 0ball")
        
        // 9. 최종적으로 3개의 숫자를 자리까지 모두 일치하게 입력(3strike)하면 게임을 종료(break)한다.
    }
    
    return 0;
}
